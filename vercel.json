{
    "version": 2,
    "builds": [
        { "src": "*.html", "use": "@vercel/static" }
    ],
    "routes": [
        { "src": "/lib/(.*)", "dest": "/lib/$1" },
        { "src": "/public/(.*)", "dest": "/public/$1" },
        { "src": "/(.*)", "dest": "/$1" }
    ]
}